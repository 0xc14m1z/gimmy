defmodule Engine.Offer.SubscriptionTypes do

  def create( name, months, reception_commission, teacher_commission,
              discipline_id, category_id \\ nil) do

  end

  def update(id, attributes) do

  end

  def delete(id) do

  end

end
